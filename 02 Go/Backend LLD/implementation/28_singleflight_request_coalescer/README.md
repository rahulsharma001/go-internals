# 28. Singleflight Request Coalescer

Prompt-first Go package for [[Singleflight Request Coalescer]]. The initial files intentionally compile without claiming implementation evidence. Add behavior only during a timed attempt, then record tests and review dates in the tracker.

Package: singleflight_request_coalescer  
Verify: go test ./...  
Concurrent package: yes; also run go test -race ./...
