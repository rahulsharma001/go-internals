# 21. Token-Bucket Rate Limiter

Prompt-first Go package for [[Token-Bucket Rate Limiter]]. The initial files intentionally compile without claiming implementation evidence. Add behavior only during a timed attempt, then record tests and review dates in the tracker.

Package: token_bucket_rate_limiter  
Verify: go test ./...  
Concurrent package: yes; also run go test -race ./...
