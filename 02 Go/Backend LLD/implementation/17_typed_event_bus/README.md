# 17. Typed Event Bus

Prompt-first Go package for [[Typed Event Bus]]. The initial files intentionally compile without claiming implementation evidence. Add behavior only during a timed attempt, then record tests and review dates in the tracker.

Package: typed_event_bus  
Verify: go test ./...  
Concurrent package: yes; also run go test -race ./...
