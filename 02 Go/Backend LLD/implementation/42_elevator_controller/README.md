# 42. Elevator Controller

Prompt-first Go package for [[Elevator Controller]]. The initial files intentionally compile without claiming implementation evidence. Add behavior only during a timed attempt, then record tests and review dates in the tracker.

Package: elevator_controller  
Verify: go test ./...  
Concurrent package: yes; also run go test -race ./...
