# 16. In-Process Pub Sub Broker

Prompt-first Go package for [[In-Process Pub Sub Broker]]. The initial files intentionally compile without claiming implementation evidence. Add behavior only during a timed attempt, then record tests and review dates in the tracker.

Package: in_process_pub_sub_broker  
Verify: go test ./...  
Concurrent package: yes; also run go test -race ./...
