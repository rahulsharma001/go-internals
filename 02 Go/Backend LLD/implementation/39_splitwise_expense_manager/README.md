# 39. Splitwise Expense Manager

Prompt-first Go package for [[Splitwise Expense Manager]]. The initial files intentionally compile without claiming implementation evidence. Add behavior only during a timed attempt, then record tests and review dates in the tracker.

Package: splitwise_expense_manager  
Verify: go test ./...  
Concurrent package: yes; also run go test -race ./...
