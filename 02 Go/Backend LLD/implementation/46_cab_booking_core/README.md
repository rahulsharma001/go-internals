# 46. Cab-Booking Core

Prompt-first Go package for [[Cab-Booking Core]]. The initial files intentionally compile without claiming implementation evidence. Add behavior only during a timed attempt, then record tests and review dates in the tracker.

Package: cab_booking_core  
Verify: go test ./...  
Concurrent package: yes; also run go test -race ./...
