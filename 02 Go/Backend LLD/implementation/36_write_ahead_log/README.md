# 36. Write-Ahead Log

Prompt-first Go package for [[Write-Ahead Log]]. The initial files intentionally compile without claiming implementation evidence. Add behavior only during a timed attempt, then record tests and review dates in the tracker.

Package: write_ahead_log  
Verify: go test ./...  
Concurrent package: yes; also run go test -race ./...
