# 10. Semaphore With Context Cancellation

Prompt-first Go package for [[Semaphore With Context Cancellation]]. The initial files intentionally compile without claiming implementation evidence. Add behavior only during a timed attempt, then record tests and review dates in the tracker.

Package: semaphore_with_context_cancellation  
Verify: go test ./...  
Concurrent package: yes; also run go test -race ./...
