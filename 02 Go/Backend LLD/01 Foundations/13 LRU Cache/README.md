# 13. LRU Cache

Prompt-first Go package for [[LRU Cache]]. The initial files intentionally compile without claiming implementation evidence. Add behavior only during a timed attempt, then record tests and review dates in the tracker.

Package: lru_cache  
Verify: go test ./...  
Concurrent package: yes; also run go test -race ./...
