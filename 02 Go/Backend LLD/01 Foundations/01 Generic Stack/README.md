# 1. Generic Stack

Prompt-first Go package for [[Generic Stack]]. The initial files intentionally compile without claiming implementation evidence. Add behavior only during a timed attempt, then record tests and review dates in the tracker.

Package: generic_stack  
Verify: go test ./...  
Concurrent package: no by default
