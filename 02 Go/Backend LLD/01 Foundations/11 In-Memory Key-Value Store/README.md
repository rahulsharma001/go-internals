# 11. In-Memory Key-Value Store

Prompt-first Go package for [[In-Memory Key-Value Store]]. The initial files intentionally compile without claiming implementation evidence. Add behavior only during a timed attempt, then record tests and review dates in the tracker.

Package: in_memory_key_value_store  
Verify: go test ./...  
Concurrent package: yes; also run go test -race ./...
