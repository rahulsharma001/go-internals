# 2. Generic Queue

Prompt-first Go package for [[Generic Queue]]. The initial files intentionally compile without claiming implementation evidence. Add behavior only during a timed attempt, then record tests and review dates in the tracker.

Package: generic_queue  
Verify: go test ./...  
Concurrent package: no by default
