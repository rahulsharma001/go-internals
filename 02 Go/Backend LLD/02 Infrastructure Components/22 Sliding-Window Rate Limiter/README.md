# 22. Sliding-Window Rate Limiter

Prompt-first Go package for [[Sliding-Window Rate Limiter]]. The initial files intentionally compile without claiming implementation evidence. Add behavior only during a timed attempt, then record tests and review dates in the tracker.

Package: sliding_window_rate_limiter  
Verify: go test ./...  
Concurrent package: yes; also run go test -race ./...
