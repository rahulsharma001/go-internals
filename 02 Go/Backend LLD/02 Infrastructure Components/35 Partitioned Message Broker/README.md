# 35. Partitioned Message Broker

Prompt-first Go package for [[Partitioned Message Broker]]. The initial files intentionally compile without claiming implementation evidence. Add behavior only during a timed attempt, then record tests and review dates in the tracker.

Package: partitioned_message_broker  
Verify: go test ./...  
Concurrent package: yes; also run go test -race ./...
